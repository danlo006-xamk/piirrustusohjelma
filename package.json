{
  "name": "piirrustusohjelma",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx prisma init --datasource-provider mysql && npx prisma db pull && npx prisma generate"
  },
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.11.0",
    "express": "^4.18.2",
    "dotenv": "16.0.3"
  },
  "devDependencies": {
    "prisma": "^4.11.0"
  }
}
